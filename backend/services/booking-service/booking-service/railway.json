{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && mvn clean package -DskipTests -pl services/booking-service/booking-service -am",
    "watchPatterns": ["backend/services/booking-service/**"]
  },
  "deploy": {
    "startCommand": "cd backend/services/booking-service/booking-service && java -jar target/*.jar",
    "restartPolicyType": "ON_FAILURE"
  }
}